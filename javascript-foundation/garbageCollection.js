const number = 600; // allocate memory for number
const text = `Some text`; // allocate memory for a string

function subtractTwo(num) {
    var human = { // allocate memory for an object and his values
        first: `Ronaldinho`,
        last: `Gaucho`
    }
    return num - 2;
}

function calculate() {
    const sumTotal = 4 + 5;
    return subtractTwo(sumTotal);
}
